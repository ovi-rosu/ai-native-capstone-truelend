{"command":"node -e \"\nconst fs=require('fs');const {execSync}=require('child_process');\nlet same=true;\nfor(const g of 'ABCDEFGHIJKLM'.split('')){\n  const now=JSON.stringify(JSON.parse(fs.readFileSync('sprint-contracts/'+g+'.json','utf8')).contract);\n  const old=JSON.stringify(JSON.parse(execSync('git show HEAD:sprint-contracts/'+g+'.json',{encoding:'utf8'})).contract);\n  if(now!==old){same=false;console.log('contract',g,'CHANGED');}\n}\nconsole.log('13 frozen contracts still unchanged after re-freeze:',same);\n\" && node .claude/scripts/bundle-check.js --mode implementable 2>&1 | tail -1 && cd backend && uv run pytest -q 2>&1 | tail -2","hashes":{}}
