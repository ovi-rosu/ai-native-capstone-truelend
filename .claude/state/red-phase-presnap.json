{"command":"for f in test_log_redaction test_correlation_id test_error_envelope test_health_probe; do echo \"$f: $(grep -c '^def test_' backend/tests/unit/$f.py) test defs\"; done && echo \"=== collected total ===\" && cd backend && uv run pytest --collect-only -q 2>&1 | tail -2","hashes":{}}
