{"command":"cd /c/Users/rosuo/WORK/ai-native-capstone-truelend/backend && uv run pytest -q 2>&1 | tail -2 && uv run mypy src/ 2>&1 | tail -1 && uv run python -c \"\nfrom src.config.settings import Settings\ns = Settings(_env_file=None)\nprint('database_url default:', repr(s.database_url), '| jwt_secret default:', repr(s.jwt_secret))\nprint('jwt_ttl_seconds:', s.jwt_ttl_seconds, '(D-M: 60 min)')\n\" 2>&1 | tail -3","hashes":{}}
