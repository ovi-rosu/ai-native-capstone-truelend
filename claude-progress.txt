=== AC-RENUMBERING: why the two approved acceptance criteria are NOT added yet ===
Adding E15-S1-AC6 (duration histogram) and E15-S1-AC7 (log/metrics input
sanitisation) was approved, attempted, and REVERTED on 2026-09-14 because the
downstream cost is far larger than the change.

E15-S1 is the FIRST story in story-traces.json, so the matrix is numbered from
its acceptance criteria outward. Inserting two ACs there shifted every later VM
id by two. Verified concretely while the change was in place:
  - matrix said VM-012 -> E9-S1-AC2, but test-plan.md still said QA-VM-012 -> VM-012
    for E1-S1-AC1; VM-014 and VM-105 likewise pointed at different ACs
  - sprint-contracts/*.json are frozen against the OLD ids
  - test-plan.md is not regenerated by test-plan-write --force (by design: it only
    rewrites a filled plan under --reset-plan), so it silently went stale
That combination is a vacuous-verification hazard: a frozen check would report
PASS for an acceptance criterion it is no longer testing.

The AC source of truth for the matrix is specs/stories/story-traces.json (.acs),
plus the embedded acceptance_criteria list in specs/stories/stories.json. Editing
specs/stories/acceptance-criteria.json alone changes nothing.

Options, needing a decision:
  (a) Accept the renumber: add the ACs, run test-plan-write --force --reset-plan,
      re-fill all 84+ Observe cells in test-plan.md, re-freeze all 13 contracts,
      re-record the spec and test receipts, re-seal. Correct, but redoes the whole
      test-planning review surface.
  (b) Put the two ACs on a LATER story (e.g. a new E15-S4) so no existing VM id
      moves. Cheapest, keeps every frozen contract valid, at the cost of splitting
      E15-S1's observability concerns across two stories.
  (c) Defer both to the /sprint lane after group M, where a re-render is expected.
Both behaviours the ACs would pin are already implemented and tested (metrics
label escaping, route-template cardinality, single-line JSON log records); what is
missing is the traceable acceptance criterion, not the behaviour.

=== DO NOT RUN: spec-render-write.js (destructive after implementation) ===
`node .claude/scripts/spec-render-write.js` is destructive once any story has been
implemented. Verified 2026-09-14 by reading the script, before running it:
  - line 156 writes specs/stories/{id}.md from renderStoryMd(...) for EVERY story,
    and the renderer contains ZERO occurrences of "pending" -- it emits a fresh
    Generation Contract skeleton. Running it would replace all 29 authored
    `### Operations` sections, i.e. the entire /implement fan-out (7 agents).
  - the same run rebuilds features.json with passes:false on every feature,
    discarding evaluator results (12/107 passing at the time).
It is a renderer: idempotent only while the story graph is still generated output.
After /implement writes Operations into the markdown, the markdown is no longer
derived -- it holds hand-authored content spec-decisions.json does not contain.

To add or change an acceptance criterion after implementation has begun, hand-edit
the chain instead of re-rendering:
  1. the `## Acceptance Criteria` list in specs/stories/{id}.md
  2. specs/stories/acceptance-criteria.json (the AC index)
  3. features.json -- append, preserving existing `passes` values
  4. node .claude/scripts/test-plan-write.js --force  (matrix + traces; merges
     reviewed required_layers/checks by ac_id and will not overwrite a filled
     test-plan.md unless --reset-plan is also passed)
  5. re-record the spec and test receipts, contract-freeze.js, plan-seal.js write
Check the same hazard before running any *-render-write.js mid-build.

=== Session 7 ===
date: 2026-09-14T18:05:00.000Z
mode: /gate --group A (round 4, on-demand pre-merge)
groups_completed: []
groups_remaining: [A (GATE = BLOCK, 2 findings), B, C, D, E, F, G, H, I, J, K, L, M]
current_group: A (gated, not merged)
current_stories: [E15-S1, E9-S1, E11-S1, E15-S4]
sprint_contract: sprint-contracts/A.json (frozen; 13/13 hashes byte-identical)
last_commit: f3c25eb "fix(observability): stop the metrics scrape diluting its own error rate"
features_passing: 14 / 109 (3/3 frozen API checks, 14/14 story ACs, unanimous across 3 instances)
coverage: backend 108 tests pass (was 104), frontend 20 pass (was 18), ruff/mypy/eslint/tsc clean
learned_rules: 0
blocked_stories: none

GATE VERDICT: BLOCK -- on 2 findings, BOTH ESCALATED BY THE GATE LEAD.
NO REVIEWER RAISED A BLOCK. Full record in .claude/state/handoff/gate-group-A.md;
vote trail in specs/reviews/reverify-votes.json.

  security    PASS  unanimous 3/3, 0 BLOCK
  functional  PASS  unanimous 3/3, 3/3 frozen, 14/14 ACs
  code-review PASS  0 BLOCK, 18 WARN, 9 INFO  (round 3: BLOCK on CR-301)

ROUND 3'S REMEDIATION GENUINELY WORKED. CR-301 CLOSED, confirmed by five instances
including the reviewer who raised it (3,000 unique method tokens + unique paths ->
1 counter series / 1,713-byte body, vs round 3's 2,998+2,998 series / 3.5 MB; bounded
under BOTH parsers). CR-303/V-0 CLOSED -- round 3's identity mutant on _escape_label is
now KILLED. B-4/CR-304 performance CLOSED and proven CORRECT, not just fast (0
disagreements vs an independent reference grouper over 2,400 cases; 0 mismatches across
200,000 random amounts; byte-identical to the old regex for n=1..60; the 250 ms test has
50-80x headroom and is NOT flaky).

THE TWO BLOCKS (lead escalation of triple-confirmed WARNs, recorded as such):

  GATE-B1  test_observability_contract.py:87 -- the ONLY control on E15-S4-AC1 is
    INVERTED, not merely permissive. The assertion
    `p95_bound == "+Inf" or float(p95_bound) <= 0.5` ACCEPTS the worst case and REJECTS
    the merely bad one. Proven 3x independently: eval-1 forced a real p95 of 11 s (22x
    budget) -> 10 passed in 22.08 s (the runtime proves the sleeps ran); eval-2 found the
    all-into-+Inf mutant SURVIVES while all-into-10s is killed; code-review drove every
    bound to 1e-12 with p95 in +Inf -> 10 passed. FIX = 1 line. eval-1 independently
    recommended BLOCK on exactly this.

  GATE-B2  middleware.py:113 -- f3c25eb FIXED THE WRONG HALF OF ITS OWN PROBLEM. It
    excluded /metrics from the RED counter but NOT from the latency histogram
    (observe_duration is still called unconditionally), so an anonymous scraper still
    dilutes p95 while no longer leaving ANY trace in http_requests_total. code-review:
    reported histogramP95 5 ms vs business-only 4,875 ms on a 500 ms budget. sec-3: a
    genuine 975 ms breach reads 4.8 ms PASS after 2,000 anonymous scrapes, errorRate
    0.0000% throughout. FIX = 1 line (apply _SLI_EXCLUDED_ROUTES to observe_duration).

WHY THESE BLOCK: E15-S4 exists in this group for exactly one purpose -- to make the
declared runtime SLO measurable. As shipped the metric under-reports a real breach by
~1000x and the test guarding it cannot fail. Merging banks a "runtime SLO is measurable"
claim that is neither correct nor protected -- the same vacuous-verification hazard this
project already accepted as grounds for reverting approved work (see AC-RENUMBERING at
the top of this file).

REQUIRED IN THE SAME FIX CYCLE (not independently blocking; same two files):
  V2-01 middleware.py:193 -- changing the `else 500` default to `else 200` leaves 108/108
    GREEN. The input to the SLO error-rate gate is unprotected and the branch is live.
  V2-03 middleware.py:128 -- _sum never accumulating leaves 108/108 GREEN; only substring
    presence is asserted. Breaks rate(sum)/rate(count).
  CR-401/CR-314 middleware.py:175 -- seen.append precedes await send, so a send raising at
    response.start records 200 WHILE THE CLIENT GOT 500. ab0fd6e moved it, didn't fix it.
  CR-402 middleware.py:210 -- two _observe sites, not a finally: measured
    http_requests_total 2 vs _count 1 from ONE request when observe_duration raises.
  CR-407/CR-408 -- the new cardinality tests cover record_request() only, NOT the HTTP
    boundary; the `route="/metrics" not in body` assertion is vacuous and false on a
    second scrape.
  NEW-2/V2-05/CR-411 middleware.py:89 -- method_label uppercases BEFORE the membership
    test, so lowercase `get` (405) records method="GET" status="405", an impossible event;
    "OPTION" plus U+017F folds onto OPTIONS. Cardinality bounded, fidelity not.
  NEW-1 middleware.py:193 -- measured REGRESSION from ab0fd6e: an outer task cancelled
    mid-request records (GET,/slow,500) where ab0fd6e~1 recorded nothing. Reachable at
    uvicorn's graceful-shutdown timeout, so shutdown injects false 5xx into the SLO.

CR-306 IS WORSE THAN RECORDED and is now a MONEY-CORRECTNESS issue, not a doc gap:
"12.345" -> 200 silently rounded to "12.35"; "1e2" -> "100.00"; "0.001" -> "0.00";
"1234" -> "1234.00". Confirmed live by all three evaluators. Latent (MoneyField has zero
production call sites).

REFUTATIONS -- STOP RE-LITIGATING:
  CR-311 REFUTED. Prior round reasoned from the enumeration code and never tested
    propagation. A late logger has no filter of its own BUT propagate=True and the ROOT
    handler carries RedactionFilter -> [REDACTED]. The 3 "unfiltered" entries are
    logging.PlaceHolder namespace nodes, not loggers.
  CR-313 DOWNGRADED to latent. 11 orderings all 108 pass. Critically pytest-randomly is
    NOT INSTALLED, so round 3's `-p no:randomly` was a NO-OP -- the prior ordering
    evidence did not test what it claimed.
  AC-RENUMBERING HAZARD REFUTED FOR GROUP A. All 14 ACs map 1:1 to
    VM-001..005/009..011/070..073/108..109, verbatim descriptions, one trace per AC. No id
    points at a different AC than its test claims. This de-risks the deferred AC decision.
  E15-S1-AC1 IS NOT VACUOUS (corrects rounds 2-3). The filter-to-no-op mutant kills 7
    tests; scrub_text-to-identity kills 2. The gap is production WIRING, not oracle
    strength.
  REDACTION BYPASS BREADTH partly refuted. Round 3's "ALL forms bypass" is wrong: plain,
    4-space, dash, lowercase DO redact; 12 of 18 bypass. Realistic leakers for Indian KYC:
    NBSP, soft hyphen, and a 5-SPACE RUN -- the last NEW, because it defeats the 0-to-4
    bound added to fix SEC-001. Also BIDIRECTIONAL.
  SEC3-011 fix is BIGGER than thought: converting to InvalidMoneyAmountError would NOT
    give 422 -- no handler maps ValueError, so /probe/typed also 500s. Needs BOTH a
    DecimalException catch AND a 4xx mapping. Boundary: 1E+999995 handled, 1E+999998 and
    beyond leak.
  The 500 default is NOT an attacker vector (uvicorn does not cancel on client disconnect:
    5 aborted curls -> 6x status=200, zero 5xx). It IS a shutdown-time regression (NEW-1).
  Money.multiply timing dispute SETTLED: both rounds were right about DIFFERENT scalars.
    Decimal scalar 0.00 ms; 10**1000000 = 9,538 ms of which 9,531 ms is CPython's
    int->Decimal COERCION, not the multiplication.
  NO CONTRACT DRIFT: 13/13 hashes byte-identical. The apparent mismatch is core.autocrlf
    (raw df24b294... vs LF-normalised a0625d4b..., the latter matching the receipt).

STILL BEFORE GROUP B/E: F-1 redaction WIRING (zero register_sensitive call sites in
backend/src; ba457bf put them in specs/bundles/*.json, the SPECS not the code -- all six
instances confirmed, two logged live raw PAN+Aadhaar); normalize-then-match (NFKC);
SEC2-004 (the tripwire the deferral rests on is ITSELF bypassable -- misses national_id/
uid/kyc_number/tax_id/document_number, and a call named only in a COMMENT passes, while
expand/company false-positive, so the cheapest way to silence it IS the bypass);
SEC4-002/SEC3-003 residual (harness-owned: errorRate sums every series with no route
dimension, so 50 real errors read 0.2488% after 20,000 anonymous /health -- only ~4,900
requests needed); CR-409/SEC4-003 (frontend Money unbounded: 1e400000000 -> V8 OOM exit
134 from an 11-BYTE input, while the backend rejects anything above 1e25 in 0.0 ms);
FE-GUARD (the frontend float guard has NO DIVISION pattern, though principal/months is
exactly the EMI shape); SEC4-004 (X-Request-ID is structurally un-scrubbable --
JSONLogFormatter injects request_id AFTER RedactionFilter runs); CR-306; and PIN
--http httptools at E15-S2.

HARNESS DEFECTS. GI-005 FIXED: per-instance git worktrees eliminated round 3's mutant
leakage; all seven instances verified their own tree pristine. But the fix exposed new
defects, and one of them is a vacuity risk:
  GI-010 (HIGH) Agent(isolation:"worktree") provisions from origin/main, NOT the session
    HEAD. All seven branches: "Created from origin/main" at c4189ec -- a README-only commit
    18 BEHIND, with no backend/ or frontend/ AT ALL. All seven detected and reset it, but an
    instance that did NOT check would have found nothing to review and could have returned a
    CLEAN PASS ON AN EMPTY TREE -- vacuous verification indistinguishable from genuine in the
    vote trail. Fix: provision from reviewed HEAD; assert a sentinel path + resolved HEAD
    before dispatch. ALWAYS verify each instance's head field.
  GI-013 Worktree isolation does NOT isolate the OS process table or port space. sec-2 ran
    a `taskkill /F /IM python.exe` (killing every Python process while two evaluators had
    live servers) and found an unidentified process on its assigned port 8022.
  GI-011 pre-write-gate.js normalises .claude/worktrees/<id>/specs/reviews/... and fails to
    match its own specs/reviews/** allow-entry (allowed_paths resolve against the MAIN root).
    sec-3 declined to bypass and reported inline (lead transcribed its artifacts); eval-2 used
    a Python heredoc; sec-1's write SUCCEEDED -- so the block is INCONSISTENT, not absolute.
  GI-014 The context pack was written but NOT COMMITTED, so every worktree instance read the
    committed ROUND-3 pack. eval-3 caught it (re-measured 108/20 vs the pack's 104/18). The
    round-4 framing survived only because it was duplicated in each dispatch prompt. Fix:
    commit the pack, or write it into each worktree, before dispatch.
  GI-015 specs/brownfield/code-graph.json is EMPTY (status "empty", reason "source index
    unavailable", generated 2026-09-13 BEFORE any source existed) and has never been rebuilt.
    Both mandated human trust surfaces are therefore VACUOUS: docs/CODEBASE.md reports 0
    indexed files / 0 edges / 0 concepts, and walkthrough.md's "Blast radius" reports no
    graph neighbors. specs/brownfield/** is OUTSIDE the task envelope, so this needs a
    deliberate /code-map run.
  GI-012 slo-verdict.json reports p95_ms 4.75 -- the same few-ms figure the GATE-B2 attack
    produces. The SLO row measures the OBSERVER, not the application. Its "pass" is
    substantially vacuous.
  GI-016 pre-bash-gate.js scans the RAW command string including heredoc BODIES, so prose
    containing a greater-than sign followed by a token is parsed as a shell redirection and
    BLOCKED (hit while writing this very entry: the text "above 1e25" written in its
    original arrow form was rejected as "Bash write to 1e25"). It also rejects writes to the
    session scratchpad. Workaround: write prose with the Write tool, not a Bash heredoc.
  RECONFIRMED: GI-001, GI-002, GI-003, GI-004, GI-006, GI-007, GI-009.
  GI-008 DID NOT BITE this round -- the evaluator row parsed as pass.

ALSO RECONFIRMED (learned rule): contract-freeze.js --verify SILENTLY IGNORES the unknown
flag and RE-FREEZES. Only the timestamp changed (all 13 hashes identical, which incidentally
proved the contracts unmodified); the receipt was restored with git checkout.

quality-card: FAIL, 8 pass / 2 fail. Both failing rows are the known artifacts -- ownership
(GI-006 waiver not applied by the runner; only backend/src/__init__.py, ratified + unexpired)
and regression (GI-002; correctly scoped it PASSES, see
specs/reviews/local-regression-gate-verdict.json). security_scan scores "unknown" (GI-009).
finalize-task-evidence correctly BLOCKED: signed_approvals 0/2, threat_model stale,
sast/dependency_scan unprovisioned; the unit/acceptance/integration "failures" all resolve to
gate-checks.json being pass:false from the three registry artifacts -- NOT from failing tests.
Lifecycle left ACTIVE, deliberately NOT sealed.

UNSCANNED, NOT PASSED: semgrep (SAST), gitleaks (secrets), pip-audit (Python CVE) all
unprovisioned. npm audit against frontend/ explicitly: 1 critical + 1 high + 3 moderate, ALL
devDependency-only, prod dependency count 5. Three inferential security instances were again
the only real coverage for the injection/authz/PII classes.

next_action: Run /test
  raised no reviewer BLOCK on any axis. E1-S1 has begun: dependencies added (sqlalchemy,
  alembic, pyjwt, psycopg) and Operations steps 1-2 landed (types/enums.py,
  types/identity.py). Continue E1-S1 from step 3 (SQLAlchemy declarative base + table
  registry). Before the next /gate --group A: commit the context pack FIRST (GI-014) and
  ASSERT EACH INSTANCE'S RESOLVED HEAD before trusting its verdict (GI-010) -- worktrees
  provision from origin/main, not session HEAD, so an instance can silently review an empty
  tree and return a clean PASS. The F-1 redaction wiring lands with E1-S1 step 10 and E4-S1
  step 6, which now specify register_sensitive explicitly. Consider /code-map to un-vacuum
  the two human trust surfaces (GI-015). HUMAN-ONLY: Docker Desktop for E15-S2's compose
  criterion; semgrep/gitleaks/pip-audit are absent, so SAST and dependency tiers are
  UNSCANNED, not passed.

=== Session 6 ===
date: 2026-09-14T13:40:00.000Z
mode: /gate --group A (round 3, on-demand pre-merge)
groups_completed: []
groups_remaining: [A (GATE = BLOCK, 1 finding), B, C, D, E, F, G, H, I, J, K, L, M]
current_group: A (gated, not merged)
current_stories: [E15-S1, E9-S1, E11-S1, E15-S4 (new)]
sprint_contract: sprint-contracts/A.json (re-frozen after E15-S4 appended; 13/13 hashes verified)
last_commit: ba457bf "fix(spec): put the redaction call sites in the Operations that introduce PII"
features_passing: 14 / 109 (F108/F109 added and passing; 0 drift outside group A)
coverage: backend 104 tests pass (was 88), frontend 18 pass, ruff/mypy/eslint/tsc clean
learned_rules: 0
blocked_stories: none

GATE VERDICT: BLOCK -- on ONE finding. Full record in
.claude/state/handoff/gate-group-A.md; vote trail in specs/reviews/reverify-votes.json.

Both voted axes FLIPPED to PASS from round 2:
  security    PASS, unanimous 3/3, 0 BLOCK  (round 2: BLOCK 3/3)
  functional  PASS, unanimous 3/3           (round 2: FAIL 2/3)
  code-review BLOCK (single instance, outside the vote, sufficient alone)

Round-2 BLOCKs: B-1 CLOSED, B-3 CLOSED, B-2 HALF-CLOSED, B-4 OPEN (downgraded).

THE ONE OPEN BLOCK -- CR-301 (backend/src/api/middleware.py): the "method" label is
unbounded. f1367f5 bounded the ROUTE dimension (verified to 60k paths: 3 route values,
+376 KB) but method still flows into the keys of _request_counts and the NEW
_duration_counts/_duration_totals, process-global and never evicted, behind an
unauthenticated /metrics. Under --http h11: 3,000 methods -> 2,998+2,998 series /
3.5 MB body; 22,003 methods -> 25.3 MB retained. Refuted for the SHIPPED config
(uvicorn[standard] + uv.lock -> httptools/llhttp answers 400 to unknown methods below
ASGI; ceiling is llhttp's ~35-method table). Held BLOCK because nothing documents, tests
or pins that parser, middleware.py is deliberately server-agnostic, and no Dockerfile
exists yet. FIX = 3 lines mirroring _UNMATCHED_ROUTE; also closes CR-302.

B-4 (money.ts:78 quadratic regex) is UNTOUCHED by all six remediation commits and was
reproduced at full magnitude by 5 instances: format() 4,088-4,301 ms from 8 bytes,
421,224-429,464 ms (~7 min) from 9 bytes. Round 2's refutation is now EXPLAINED: it timed
toFixed(), a LINEAR sink at 0.6-3.2 ms, 1,260-7,000x cheaper than the real one. Downgraded
BLOCK -> WARN by 4 independent reachability analyses (format()'s only caller is MoneyText,
whose only callers are tests; backend Money._quantize rejects past 28 digits). eval-3
dissents toward high because MoneyText.tsx:10-15 declares the prop as Money-or-string and
calls fromWire(prop).format() with no bound. One-line fix -- take it now rather than carry
it a third round.

NEW, MUST NOT BE LOST:
  1. V-0/CR-303 (major): the whole B-1 escaping fix has ZERO coverage. Mutating
     _escape_label to the identity function leaves all 104 tests GREEN; both
     "labels cannot be injected from a request path" tests actually pass via
     route_label's cardinality bound. Found independently by eval-3 and code-review.
     A refactor reopens the injection silently.
  2. F-1/V-1 (major): redaction is INERT -- ZERO register_sensitive call sites in
     backend/src. eval-2 logged live raw PAN + Aadhaar from a request path and from
     X-Request-ID. f5a5ace's SEC-103 fix is correct but scrub_text is
     registration-driven, so an AppError context still egressed aadhaar_int, a
     "pan-..." KEY, and an absolute path. ba457bf put the call sites in
     specs/bundles/*.json -- the SPECS, not the code. E15-S1-AC1 therefore passes only
     as a SURROGATE. With values registered, dotted/underscored/slashed/NBSP/
     soft-hyphen/zero-width/fullwidth forms ALL still bypass; normalize-then-match is
     still required before group B/E logs applicant data.
  3. SEC3-003 (WARN, nobody assigned it): unauthenticated VOLUME DILUTION masks an SLO
     breach -- the twin of B-1's injection. errorRate() is 5xx over LIFETIME counters
     that never reset and /metrics is anonymous and unrated. A genuine 50.00% outage
     reads 0.0100% (PASSING the 1% budget) with 1e6 404s; a genuine 9,525 ms p95 reads
     4.8 ms. The fix belongs in slo-check.js: exclude the unmatched-route label it
     already emits.
  4. CR-306 (WARN): MoneyField's declared pattern is UNENFORCED -- POST principal
     "12.3" returns 200 "12.30" though the schema and test_no_float_money.py:342 both
     call 1dp invalid. A real defect in a807678.
  5. CR-302/F-3 (WARN): _escape_label passes raw CR, TAB, NUL, BEL, ESC, DEL.
     _histogram_lines() emitted 14 lines carrying a raw CR, contradicting E15-S4-AC2's
     own "no raw control character". Unreachable under httptools; same root cause as
     CR-301.
  6. Disputes SETTLED: Money.multiply leaks bare decimal.Overflow in 0.0 ms -- there is
     NO DoS; it is a 500-instead-of-422 contract break (neither round-2 timing was
     right). "AC2 passes by construction" is PARTIALLY REFUTED -- the root-only mutant
     kills both AC2 tests, so "vacuous" overstated it.

E15-S4 DELIVERED ITS POINT: the buckets are verified genuinely cumulative (proved with a
700 ms observation), eval-2 recovered p95 884.62 ms against 934.22 ms empirical truth and
correctly flagged a 500 ms breach, and slo-check.js now reports p95_ms 4.75 where it was
permanently null. The declared runtime SLO is measurable for the first time.

quality-card reads 7 pass / 3 fail, but only code_review is a REAL failing row. The
"evaluator" row is a parser artifact (GI-008: md_verdict regexes the whole document and
takes the FAIL branch because the words BLOCK/FAIL appear in DISCUSSION at lines 26, 338,
649, while line 12 declares FUNCTIONAL VERDICT: PASS). The "ownership" row is covered by
a ratified waiver naming backend/src/__init__.py exactly (expires 2026-10-15).

HARNESS DEFECTS. Reconfirmed: GI-001 security-scan.js with --all --staged --boundary-only
scans ZERO files and self-reports a clean pass, and runDeps() probes only the repo root so
npm audit never reaches frontend/ -- always pass explicit --files. GI-002 the registry
invokes regression-gate.js with only --replay, never --exclude-group, so it grades the
UNBUILT groups B-M as prior baselines; correctly scoped it PASSES. GI-003 canvas-sync has
no harness-state exclusion and blocks on .claude/state/red-phase-presnap.json; product
scope is clean (13 files, 0 missing).
New: GI-004 the skill's documented "npm run sensor-waivers" ENOENTs because there is no
root package.json -- the real validator is .claude/scripts/validate-sensor-waivers.js
(pass, 5 waivers, none expired). GI-005 (HIGHEST PRIORITY) concurrent 3-instance
re-verification in ONE shared working tree is UNSAFE: instances mutation-test production
files simultaneously, 3 of 7 agents reported seeing a sibling's live MUTANT edit to
middleware.py/routes.py, and the canonical security instance had to git-checkout two live
mutants before it could re-derive its verdicts; npm test transiently read 20 tests instead
of 18 and repo-wide ruff transiently failed on a sibling scratch dir. Fix: give each
instance its own git worktree, not merely its own port. GI-007 evidence-integrity passes
with applicable:false, which is honest but VACUOUS for group A -- do not read it as
browser-backed verification. GI-008 quality-card's md_verdict parser cannot separate a
report's verdict from its content. GI-009 security-scan.json carries no pass field so the
card scores it unknown, and it lists only gitleaks+semgrep as missing while pip-audit is
absent too.

finalize-task-evidence correctly BLOCKED: signed_approvals 0/2, threat_model stale,
sast/dependency_scan unprovisioned. The unit/acceptance/integration "failed" entries all
resolve to gate-checks.json, which is pass:false only because of the three registry BLOCKs
above -- NOT because tests fail (104 backend + 18 frontend pass). Lifecycle left ACTIVE,
deliberately NOT sealed.
UNSCANNED, NOT PASSED: semgrep (SAST), gitleaks (secrets) and pip-audit (Python CVE) are
all unprovisioned. Three inferential security instances were the only real coverage for
the injection/authz/PII classes this round.

next_action: Fix CR-301 (3 lines -- clamp the method label to a known-method set with one
  "other" bucket) and B-4 (1 line -- bound the digit count before the separator regex),
  add the two mutation-killing tests (V-0/CR-303 for _escape_label, W-5 for the
  non-cumulative-bucket p95 test), then re-run /gate --group A, ideally with one git
  worktree per instance (GI-005). Do NOT build group B until CR-301 is closed. Before
  group B also: normalize-then-match redaction (F-1), slo-check.js excluding the
  unmatched-route label (SEC3-003), CR-306's unenforced MoneyField pattern, and PIN
  --http httptools when E15-S2 writes the Dockerfile or CR-301 goes live regardless of
  the app-level fix.

=== Session 5 ===
date: 2026-09-14T12:20:00.000Z
mode: /gate --group A (round 2, on-demand pre-merge)
groups_completed: []
groups_remaining: [A (GATE = BLOCK), B, C, D, E, F, G, H, I, J, K, L, M]
current_group: A (gated, not merged)
current_stories: [E15-S1, E9-S1, E11-S1]
sprint_contract: sprint-contracts/A.json (frozen, untouched)
last_commit: 9112495 "fix: complete the frozen error envelope, sanitise context, ship /health and /metrics"
features_passing: 12 / 107 (last_evaluated refreshed by evaluator 1; no status field changed)
coverage: backend 88 tests pass, frontend 18 tests pass, ruff/mypy/eslint/tsc clean
learned_rules: 0
blocked_stories: none

GATE VERDICT: BLOCK. See .claude/state/handoff/gate-group-A.md for the full
record, and specs/reviews/reverify-votes.json for the vote trail.

All five prior BLOCKs (CR-001/002/003, SEC-001/002) are genuinely CLOSED,
verified by execution. The frozen contract passes: QA-VM-003/004/005 live in
3/3 evaluator instances, all 12 story ACs pass.

But the two remediation commits introduced FOUR new BLOCKs:
  1. /metrics Prometheus exposition INJECTION — one unauthenticated GET forges
     counters and flips this project's own SLO sensor to 99.9% error rate
     (budget 1%). Also works in reverse to mask a real outage.
  2. /metrics unbounded cardinality — process-global Counter keyed on the raw
     path, never evicted: 60k paths from one connection = +28.2 MB retained.
  3. errors.py:107 discards HTTPException.headers — 405 loses Allow, 401 loses
     WWW-Authenticate (RFC 9110). One-line fix.
  4. frontend money.ts:78 quadratic separator regex — 9 bytes freezes the thread
     for >100 s. Latent (no frontend entry point yet).

Axes: security BLOCK (unanimous 3/3), functional FAIL (2/3), code-review BLOCK
(out-of-vote, sufficient alone). Task lifecycle left `active`;
finalize-task-evidence.js correctly refused completion.

HARNESS DEFECT worth remembering: security-scan.js reported a clean pass having
scanned ZERO files (`--staged` with an empty index → no SAST targets, self-reports
ran:true, no skip warning; and runDeps() only looks for package.json in the repo
ROOT so npm audit never ran against frontend/). Always pass explicit --files.
Also: no dependency lockfile is tracked at all (.gitignore:48-49).

next_action: Run /auto
  together, plus the sanitise_context int/bool/key gap). THREE SPEC DECISIONS
  NEEDED FIRST because /metrics has no AC and api-contracts.md is frozen:
  (a) may /metrics require auth? (b) add an AC for a duration metric so
  slo.p95_ms is measurable? (c) add an AC for log-input sanitisation?
  Then re-run /gate --group A. Do NOT build group B on this substrate.

=== Session 0 ===
date: 2026-09-14T12:10:00.000Z
mode: full
groups_remaining: [A (SUPERSEDED — see Session 5: the fresh /gate verdict is BLOCK), B, C, D, E, F, G, H, I, J, K, L, M]
groups_completed: []
current_group: none
current_stories: []
sprint_contract: sprint-contracts/A.json
last_commit: 9112495 "fix: complete the frozen error envelope, sanitise context, ship /health and /metrics"
features_passing: 12 / 107
coverage: backend 88 tests pass, frontend 18 tests pass, ruff/mypy/eslint/tsc clean
learned_rules: 0
blocked_stories: none

GROUP A — every /gate finding is now closed across commits 5c13f54, f4abd41,
9558cc5 and 9112495. The stored code-review-verdict.json and gate-receipt.json
read pass:false and PREDATE all of them. That fresh /gate has now RUN; see Session 5
for the real verdict (BLOCK). Do not build group B yet.
  CR-001 Money accepts NaN                -> FIXED (is_finite guard)
  CR-002 float guard ignores arithmetic   -> FIXED (Div/FloorDiv + math)
  CR-003 500 loses X-Request-ID           -> FIXED (pure-ASGI middleware mounted
                                             outside ServerErrorMiddleware)
  CR-004 uvicorn loggers bypass JSON      -> FIXED (_route_server_loggers_through_root)
  SEC-001 ReDoS in the redaction pattern  -> FIXED (bounded {0,4}; separator-bearing
                                             values matched literally). Note: the gate's
                                             timings were not reproducible here across
                                             three constructed inputs; the construction
                                             was unsound regardless.
  PII leak on exception scope-escape      -> FIXED (request-scoped registration owned by
                                             the middleware, which unwinds neither
                                             contextvar on the failure path and logs the
                                             traceback itself). Root cause was deeper than
                                             reported: FastAPI runs a sync `def` handler in
                                             a threadpool on a COPY of the context, so
                                             ContextVar.set there was invisible and silently
                                             lost every value a sync handler registered. The
                                             holding var is now a mutable set.
  AppError.context egress channel         -> FIXED (scalars only, Decimal -> str,
                                             credential URIs dropped, scrubbed via a
                                             scrub_text helper shared with the log filter)
  Envelope only covered AppError          -> FIXED (404/422/500 handlers; a 422 reports the
                                             field location only, never the rejected value)
  /health missing database + version      -> FIXED as a PORT. E15-S1 owns no database
                                             access, so the field reads "unconfigured"
                                             until a probe is registered rather than
                                             hardcoding an unobservable "ok". E1-S1 step 4
                                             wires the real probe.
  /metrics 404                            -> FIXED (RED counters, method/route/status)
  money-guard whole-line mute             -> FIXED (exempts only the category it names)

OPEN — HUMAN ONLY
  1. Docker Desktop is not installed. E15-S2 is IN group B and AC1 asserts
     `docker compose up ... all three report healthy`. verification.mode is `local`
     so the evaluator boots, but that AC stays unverifiable.
  2. MoneyField has no JSON-schema hook, so /openapi.json and /docs break on the
     first money-bearing route -- also group B. Fix before that wave, not during.
  3. E1-S2 step 7's Modifies authorisation is now in place (design round 3), so
     group C no longer blocks on ownership-check for it.
  4. Session 4 design defects 2 and 4-7 remain open; each is decided at the group
     that reaches it.

next_action: Await the in-flight /gate verdict for group A, then run /auto --group B
  subject to items 1 and 2 above.

=== Session 1 ===
date: 2026-09-13T18:05:00.000Z
mode: full
groups_completed: []
groups_remaining: [A, B, C, D, E, F, G, H, I, J, K, L, M]
current_group: A (platform logging + money + delinquency types)
current_stories: [E15-S1, E9-S1, E11-S1]
sprint_contract: sprint-contracts/A.json
last_commit: 14e9487 "feat: scaffold harness and land approved BRD/spec/design/test planning artefacts"
features_passing: 0 / 107
coverage: 0%
learned_rules: 0
blocked_stories: none
next_action: Dispatch generator for group A (E15-S1, E9-S1, E11-S1), then SECTION 5 ratchet

=== Session 2 ===
date: 2026-09-13T18:20:00.000Z
mode: full
groups_completed: []
groups_remaining: [A, B, C, D, E, F, G, H, I, J, K, L, M]
current_group: A (dispatched, halted before any code)
current_stories: [E15-S1, E9-S1, E11-S1]
sprint_contract: sprint-contracts/A.json
last_commit: 14e9487 "feat: scaffold harness and land approved BRD/spec/design/test planning artefacts"
features_passing: 0 / 107
coverage: 0%
learned_rules: 0
blocked_stories: [E15-S1, E9-S1, E11-S1 (task-envelope allowed_paths excludes specs/stories/**)]
next_action: BLOCKED ON HUMAN — the task envelope must be re-scoped before any group can build. `/implement` Step 0.2 has to write the Generation Contract Operations into specs/stories/{id}.md and pre-write-gate/pre-bash-gate refuse it. Add specs/stories/**, specs/design/**, docker-compose.yml, .env.example and README.md to allowed_paths (authorized `task-envelope.js amend --allow ...`, or abort+recreate the envelope), then re-run /auto --group A. Separately decide the runtime story: docker is not installed but verification.mode=docker. Details in .claude/state/failures.md.

=== Session 3 ===
date: 2026-09-13T19:05:00.000Z
mode: full
groups_completed: []
groups_remaining: [A, B, C, D, E, F, G, H, I, J, K, L, M]
current_group: none
current_stories: []
sprint_contract: sprint-contracts/A.json
last_commit: 14e9487 "feat: scaffold harness and land approved BRD/spec/design/test planning artefacts"
features_passing: 0 / 107
coverage: 0%
learned_rules: 0
blocked_stories: []
notes: Session 2 blockers resolved. Task envelope rotated to add specs/stories/**, specs/design/reasons-canvas.md, specs/design/amendments/**, docker-compose.yml, .env.example, README.md; architecture.md, api-contracts.md, data-models.md, specs/test_artefacts/** and sprint-contracts/** stay frozen. Repaired a group A contract defect: QA-VM-003/004 observed GET /products (built by E1-S1, group B) and QA-VM-001 observed POST /applications (built by E4-S1, group E) while frozen into group A, so group A could never pass. QA-VM-003/004 retargeted to GET /health; VM-001 reverted to unit-only. Group A contract is now GET /health x3. Frozen checks 84 -> 83. Test receipt re-recorded (round 2), contracts re-frozen, plan re-sealed (49 artifacts).
next_action: Run /auto --group A. Docker Desktop install still pending and is required before group B (E15-S2 compose stack); verification.mode remains docker.
